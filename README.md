# Cache Performance Analyzer

Simple program that analyzes cache L1, L2, L3 cachce performance by randomly testing pointer chase times
with different buffer sizes.

Can see typical average latency for working with buffers of different sizes.

Memory is shuffled to try and disrupt CPU prefetching. 
